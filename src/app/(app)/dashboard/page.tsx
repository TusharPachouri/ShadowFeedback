'use client'

import { Message } from "@/model/User"
import { useState } from "react"

function Page() {
  const [messages, setMessages] = useState<Message[]>([])
  return (
    <div>
      
    </div>
  )
}

export default Page
